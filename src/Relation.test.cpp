#include <catch.hpp>

#include <Relation.hpp>

/*
TEST_CASE("test", "Relation")
{
	Relation rlt1;
	Relation rlt2;
	
	SECTION("Relation has id") {
		REQUIRE(rlt1.get_id().is_empty());
	}
	
	SECTION("Relation can has relation with two objects") {
		Element elm1;
		Element elm2;
		rlt1.connect(elm1, elm2);	// connect elm1 to elm2.
		
		REQUIRE(rlt1.get_from() == elm1);
		REQUIRE(rlt1.get_to() == elm2);
	}
}
*/