#pragma once

#include <Element.hpp>
#include <Relation.hpp>

/** TODO：目前可以不需要，以为从Relation可以找到Element。
 * 允许管理自己的Relation的Element。
 */
class BasicElement : public Element
{
public:
protected:
private:
};