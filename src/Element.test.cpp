#include <catch.hpp>

#include <Element.hpp>


/*
TEST_CASE("test", "[Element]")
{
	Element elm1;
	Element elm2;
	
	REQUIRE( ! elm2.get_id().is_empty() );
	
	SECTION( "Element has id." ) {
        REQUIRE( elm1.get_id().is_empty() );
    }
    
    SECTION( "Add relation" ) {
    	elm1.add_relation();
    }
	
}
*/